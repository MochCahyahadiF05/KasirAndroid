<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-files-path name="images" path="Pictures" />
    <cache-path name="image_cache" path="." />

    <!-- Cache directory untuk temporary files -->
    <cache-path
        name="temp_receipt_images"
        path="." />

    <!-- External files directory -->
    <external-files-path
        name="receipt_files"
        path="." />

    <!-- External cache directory -->
    <external-cache-path
        name="temp_external_files"
        path="." />

    <!-- Pictures directory untuk save gambar -->
    <external-path
        name="external_pictures"
        path="Pictures/KasirApp" />

    <!-- Downloads directory -->
    <external-path
        name="external_downloads"
        path="Download/KasirApp" />
</paths>